export enum EUserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum EUserTradingAction {
  BUY = 'BUY',
  SELL = 'SELL',
}

export enum EFollowUser {
  FOLLOW = 'FOLLOW',
  UN_FOLLOW = 'UN_FOLLOW',
}

export enum EChatHistoryType {
  TEXT = 'text',
  IMAGE = 'image',
}

export enum ESendByType {
  ASSISTANT = 'assistant',
  USER = 'user',
}
