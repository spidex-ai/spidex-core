export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum ETableAlias {
  TOKEN = 'token',
  AI_AGENT = 'aiAgent',
  GPT_THREAD = 'gptThreads',
  CHAT_HISTORY = 'chatHistories',
}
